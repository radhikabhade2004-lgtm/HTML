name: Simple CI

on: [push]

jobs:
  build:
    runs-on: ubuntu-latenst
    steps:
      - name: Get Code
        uses: actions/checkout@v3

      - name: Run Test
        run: echo "Code checked successfully"
